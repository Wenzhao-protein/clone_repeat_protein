# archive/artifacts

Snapshots of large or one-off generated outputs that were previously
committed at the repository root or under retired output folders. Kept
for reference only; do not depend on them — rerun the pipeline to
regenerate equivalent files.

| File | Origin |
|------|--------|
| `newplot.png` | Plotly export left at repo root. |
| `pae.model_idx_2.rank_0.npy` | AlphaFold-style PAE matrix output. |
| `pred.model_idx_2.rank_0.cif` | AlphaFold-style structure prediction. |
| `hurdler_three_site_combinations_df1.csv` | Snapshot of an early df1 build, originally under the retired `hurdler_analysis/output/`. |
