# archive/get_re_dict

First-iteration restriction-enzyme-dictionary workspace, originally at
the repository root with a space in its name (`get_re_ dict/`). The
maintained successor is the curation pipeline under
[`../../notebooks/utils/`](../../notebooks/utils/) writing into
[`../../data/reference_output/`](../../data/reference_output/).

Kept for reference only.
