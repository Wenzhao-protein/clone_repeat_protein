# Copy outside the repository to ~/.config/hurdler/idt.env,
# fill it locally, and run: chmod 600 ~/.config/hurdler/idt.env
IDT_CLIENT_ID=
IDT_CLIENT_SECRET=
IDT_USERNAME=
IDT_PASSWORD=

# Alternatively, omit the four fields above and provide a current bearer token.
# IDT_ACCESS_TOKEN=
