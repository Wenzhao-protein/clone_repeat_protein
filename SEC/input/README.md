# SEC/input

Raw SEC chromatogram exports (Unicorn / MATLAB).

`*.mat` files are large and gitignored (see [`../../.gitignore`](../../.gitignore));
recover them from the lab fileshare before re-running notebooks.
